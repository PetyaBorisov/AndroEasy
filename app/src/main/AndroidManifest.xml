<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- УЛУЧШЕНИЕ: Разрешение для Агента Доверия -->
    <uses-permission android:name="android.permission.BIND_TRUST_AGENT" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.AndroEasy">

        <!-- ... ваши Activity и другие компоненты ... -->
        <activity
            android:name=".SetupActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <receiver
            android:name=".DailyResetReceiver"
            android:enabled="true"
            android:exported="false" />

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:exported="true"
            android:permission="android.permission.INSTALL_GRANT_RUNTIME_PERMISSIONS" />

        <!-- УЛУЧШЕНИЕ: Регистрация службы Агента Доверия -->
        <service
            android:name=".EasyUnlockService"
            android:permission="android.permission.BIND_TRUST_AGENT"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.trust.TrustAgentService" />
            </intent-filter>
            <meta-data
                android:name="android.service.trust.trustagent"
                android:resource="@xml/easy_unlock_agent" />
        </service>

    </application>
</manifest>